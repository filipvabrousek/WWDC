import SwiftUI

/// iOS 27 SwiftUI showcase.
///
/// Every screen here demonstrates an API listed in Apple's official
/// "SwiftUI updates" for iOS/iPadOS 27 (June 2026):
/// https://developer.apple.com/documentation/updates/swiftui
///
/// NOTE: These APIs shipped at WWDC 2026. The API *names* below are taken
/// verbatim from Apple's updates index; a few exact closure signatures are
/// inferred and marked `// ⚠️ verify signature in Xcode 27`. Let autocomplete
/// confirm argument labels if the compiler complains.
@main
struct iOS27SwiftUIDemoApp: App {
    var body: some Scene {
        WindowGroup {
           // SwipeTest()
           // MyDemo()
         //   ContentView()
            // NiceToolbar()
            
            
           // ConfDia()
            
          //  MenuNice()
            
           // PressChange()
            
           // PhotosShared()
            
           // PhotosMeta()
            
            // Redempt()
            
           // MPicker()
            
           // PhotosShared()
            
            PhotosMetax()
        }
    }
}
